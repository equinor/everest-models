pipeline {
    agent { label 'si-build' }
    parameters {
        string(defaultValue: "", description: 'The komodo relase to be used', name: 'RELEASE_NAME')
        string(defaultValue: "/prog/res/komodo", description: 'Root folder for komodo', name: 'KOMODO_ROOT')
    }
    stages{
        stage('run tests') {
            steps {
                sh 'sh ci/jenkins/testkomodo.sh'
            }
        }
    }
    post {
        cleanup {
            deleteDir()
        }
    }
}
