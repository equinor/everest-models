pipeline {
    agent { label 'si-build' }
    parameters {
        string(defaultValue: "bleeding-py27", description: 'The komodo relase to be used', name: 'RELEASE_NAME')
        string(defaultValue: "/prog/res/komodo", description: 'The komodo relase to be used', name: 'KOMODO_ROOT')
    }
    stages{
        stage('run tests') {
            steps {
                sh 'sh ci/jenkins/testkomodo.sh'
            }
        }
    }
    post {
        cleanup {
            deleteDir()
        }
    }
}
